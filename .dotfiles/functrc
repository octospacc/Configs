function cdexec() {
	cd "$( dirname "$( realpath "$@" )" )"
	basename "$@"
}

function touchx() {
	touch "$@"
	chmod +x "$@"
}

function ccr() {
	gcc "$@" && ./a.out
}

function cxxr() {
	g++ "$@" && ./a.out
}

function gitacp() {
	git add .
	git commit -m "$1"
	git push
}
